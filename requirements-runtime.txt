# Runtime dependencies only - for deployment on Render/cloud
# osmium (pyosmium) is excluded - it requires C++ system libs and is only
# needed for the offline data pipeline (make all), not the live API server.

# Core web stack
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9

# Data / ML
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3

# HTTP
requests>=2.31
